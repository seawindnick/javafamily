package com.java.study.algorithm.zuo.dadvanced.advanced_class_01;

/**
 * 给定两个单词beginWord和endWord，还有一本词典是list类型。 找到所有从beginWord变到endWord的最短转换路径，变动的规则是: 1，一次只能变一个位置的字符 2，每一个转换后的word一定要在list中 3，初始时list中没有beginWord这个词
 * 比如
 * beginWord = "hit"
 * endWord = "cog"
 * wordList = ["hot","dot","dog","lot","log","cog"] 返回:
 * [
 * ["hit","hot","dot","dog","cog"],
 * ["hit","hot","lot","log","cog"] ]
 * 注意:
 * 1，返回值的类型为List<List<String>> 2，如果不存在转化路径请返回空链表(不是null) 3，所有的词一定都是相同长度的 4，所有词都是小写的a~z 5，在list中没有重复的词 6，beginWord和endWord都不是空字符串或者null
 */
public class Code_06_Word_Ladder{
}